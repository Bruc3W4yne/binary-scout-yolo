# Install PyTorch with the CUDA build that matches the target machine.
# Windows RTX 4090 example:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
torch>=2.1.0
torchvision>=0.16.0
ultralytics==8.4.37
Pillow>=10.0.0
numpy>=1.24.0
pytest>=8.0.0
